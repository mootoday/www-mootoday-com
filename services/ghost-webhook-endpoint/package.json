{
  "name": "ghost-webhook-endpoint",
  "version": "1.0.0",
  "description": "",
  "author": "Mike <@mikenikles>",
  "homepage": "",
  "license": "ISC",
  "main": "lib/index.js",
  "typings": "lib/inde.d.ts",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "lib"
  ],
  "repository": {
    "type": "git",
    "url": "git@gitlab.com:mikenikles/monorepo-template.git"
  },
  "scripts": {
    "clean": "rm -fr ./lib",
    "deploy": "gcloud beta functions deploy ghostWebhookEndpoint --allow-unauthenticated --set-env-vars=GHOST_WEBHOOK_AUTH=\"$GHOST_WEBHOOK_AUTH\" --memory 128MB --runtime nodejs8 --trigger-http",
    "predeploy": "npm run clean && npm run tslint && tsc",
    "test": "echo \"Error: run tests from root\" && exit 1",
    "tslint": "tslint -c ../../tslint.json './src/**/*.ts'"
  },
  "devDependencies": {
    "@types/express": "^4.17.1"
  }
}
